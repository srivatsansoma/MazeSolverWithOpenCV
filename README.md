# MazeSolverWithOpenCV

I have implemented common algorithms for searching a graph like A*, BFS, DFS.

Used OpenCV to find the structure of the maze and find START and END points.

To run:

python -m venv venv

source venv/bin/activate

pip install opencv-python numpy

python cv.py
